<div class="container">
  <div class="card">
    <div class="card-header">Invoice
      <strong>74586824 - <%= @order.id %></strong>
      <span class="float-right">
        <strong>Status:</strong> Delivery Pending
      </span>
    </div>

    <div class="card-body">
      <div class="row mb-4">
        <div class="col-sm-6">
          <h6 class="mb-3">From:</h6>
          <div>
            <strong><%= User.find(@order.part.user_id).company_name %></strong>
          </div>
          <div>Rudi-Dutschke-Straße 26, 10969 Berlin</div>
          <div>Email: <%= User.find(@order.part.user_id).email %></div>
          <div>Phone: +49 444 666 3333</div>
        </div>

        <div class="col-sm-6">
          <h6 class="mb-3">To:</h6>
          <div>
            <strong><%= User.find(@order.user_id).company_name %></strong>
          </div>
          <div>416 Villa Gaudelet, 75011 Paris, France</div>
          <div>Email: <%= User.find(@order.user_id).email  %></div>
          <div>Phone: +33 1 48 58 51 20</div>
        </div>
    </div>

    <div class="table-responsive-sm table-responsive-lg">
      <table class="table table-striped">
        <thead>
          <tr>
            <th class="center">#</th>
            <th>Item</th>
            <th>Description</th>

            <th class="right">Cost</th>
            <th class="center">Qty</th>
            <th class="right">Total</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td class="center">1</td>
            <td class="left strong"><%= @order.part.title %></td>
            <td class="left"><%= @order.part.description %></td>

            <td class="right">€<%= @order.part.price %></td>
              <td class="center">1</td>
          <td class="right">€<%= @order.part.price %></td>
          </tr>
        </tbody>
      </table>
    </div>


    <div class="py-4">A detailed invoice has been sent to <%= link_to current_user.email %>
    </div>
   <div class="d-flex justify-content-between">
    <%= link_to 'Back to Basket', orders_path, class: "btn btn-primary"  %>
    <%= link_to 'Browse Parts', parts_path, class: "btn btn-success"  %>
  </div>

    </div>

</div>
</div>
